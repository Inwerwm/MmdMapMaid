<Page
    x:Class="MmdMapMaid.Views.MorphInterpolationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:MmdMapMaid.Controls" xmlns:helpers="using:MmdMapMaid.Helpers"
    mc:Ignorable="d">

    <Page.Resources>
        <helpers:DoubleToIntConverter x:Key="DoubleToIntConverter" />
    </Page.Resources>

    <Grid x:Name="ContentArea">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:EditableBezierCurve Size="250" EarlierControlPoint="{x:Bind ViewModel.EarlierPoint, Mode=TwoWay}" LaterControlPoint="{x:Bind ViewModel.LaterPoint, Mode=TwoWay}" Origin="BottomLeft" TrueCurveOpacity="{x:Bind TrueCurveOpacity.Value, Mode=OneWay}"/>
        <StackPanel Grid.Column="1">
            <TextBlock x:Uid="Label_XStep"/>
            <NumberBox Minimum="2" Value="10" SpinButtonPlacementMode="Inline" />
            <TextBlock x:Uid="Label_Accuracy"/>
            <Slider Minimum="0" Maximum="1" Value="0.05" StepFrequency="0.001" />
            <TextBlock x:Uid="Label_TrueCurveOpacity"/>
            <Slider x:Name="TrueCurveOpacity" Minimum="0" Maximum="1" Value="1" StepFrequency="0.001" />
        </StackPanel>
    </Grid>
</Page>
