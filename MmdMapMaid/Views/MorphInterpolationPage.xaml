<Page
    x:Class="MmdMapMaid.Views.MorphInterpolationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:controls="using:MmdMapMaid.Controls"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:EditableBezierCurve Size="{x:Bind Size.Value, Mode=TwoWay}" EarlierControlPoint="{x:Bind ViewModel.EarlierPoint, Mode=TwoWay}" LaterControlPoint="{x:Bind ViewModel.LaterPoint, Mode=TwoWay}" Origin="BottomLeft"/>
        <StackPanel Grid.Column="1">
            <NumberBox x:Name="Size" Value="1000"/>
            <TextBlock Text="{x:Bind ViewModel.EarlierPoint, Mode=TwoWay}"/>
            <TextBlock Text="{x:Bind ViewModel.LaterPoint, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
</Page>
